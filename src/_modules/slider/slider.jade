mixin slider(klass, mode)
    if mode == 'multi'
        ._slidermulti(class=klass)
            if block
                block

    if mode == 'normal'
        ._slider(class=klass)
            if block
                block